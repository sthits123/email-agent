services:
  agent:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./src/email_assistant/tools/gmail/.secrets:/app/src/email_assistant/tools/gmail/.secrets
    restart: always
